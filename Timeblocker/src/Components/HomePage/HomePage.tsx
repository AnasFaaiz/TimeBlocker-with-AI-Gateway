import Calendar from "../TimeBlocker/Calendar";

const HomePage = () => {
    return (
        <main>
            <nav>

            </nav>
            <section>
                <Calendar />
            </section>
        </main>
    );
};

export default HomePage;